package com.trackcache

import com.facebook.react.bridge.ReactApplicationContext

abstract class TrackCacheSpec internal constructor(context: ReactApplicationContext) :
  NativeTrackCacheSpec(context) {
}
